{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",          // Название задачи
            "type": "shell",          // Тип задачи (shell для выполнения команд)
            "command": "make",        // Команда для выполнения
            "group": "build",         // Группировка задачи (build для сборки)
            "presentation": {
                "reveal": "silent"    // Скрыть вывод команды в терминале
            },
            "problemMatcher": []      // Матчер для обработки ошибок (пустой, если не нужен)
        }
    ]
}